apiversion: v1
kind: service
metadata:
  name: my-service
  labels:
    app: my-app  
spec:
  selector:
    app: my-app 
  type: NodePort
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
